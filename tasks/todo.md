# TODO — Upcoming milestones

- [ ] Milestone 2 — Configuration (env vars): `PRICING_MODE`, `GRAFTCODE_PROJECT_KEY`, `.env.example`, `.gitignore`
- [ ] Milestone 3 — HTTP API on Order Service (FastAPI): `POST /orders`, `GET /orders/{order_id}`, error handlers
- [ ] Milestone 4 — Docker, Docker Compose, Makefile
- [ ] Milestone 5 — README & documentation
