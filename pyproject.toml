[project]
name = "graftcode-homework"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.34",
    # graft-pypi-graftcode-homework must be installed separately (non-standard index):
    # uv pip install --extra-index-url https://grft.dev/simple/b4486228-d411-405d-a78c-e8521e198750__free graft-pypi-graftcode-homework==0.1.0
]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
    "httpx>=0.27",
]
