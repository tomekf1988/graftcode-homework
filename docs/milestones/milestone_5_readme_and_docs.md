# Milestone 5 — README & Documentation

## Status: pending

## Scope

Complete `README.md` per recruitment task requirements:

1. Quick start (`make run-local`, `make demo`, `make test`)
2. How to test (pytest, `curl` examples)
3. Architecture: LOCAL vs REMOTE diagram, why no REST between services
4. How GraftCode works and what is needed to enable REMOTE mode
5. Pricing rules — design decision (strategy pattern + rules engine)
6. Edge case decisions (Decimal, rounding, zero quantity, unknown customer type)
7. Error handling (retry, timeout, exception taxonomy)
8. Versioning / backward compatibility approach

## Details

See: `docs/plans/pre_graftcode_milestones.md` → Milestone 5.
